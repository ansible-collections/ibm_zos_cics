# (c) Copyright IBM Corp. 2023
# Apache License, Version 2.0 (see https://opensource.org/licenses/Apache-2.0)
---
- name: Validate return data

  hosts: "all"
  gather_facts: false
  vars:
    data_set_name: "DFHINTRA"
    data_set_path: "{{ region_data_set_path }}.{{ data_set_name }}"

  module_defaults:
    ibm.ibm_zos_cics.intrapartition:
      space_primary: 4
      space_type: "M"
      region_data_sets:
        template: "{{ region_data_set_path }}.<< data_set_name >>"
      cics_data_sets:
        sdfhload: "{{ cics_steplib }}"

  tasks:
    # #############################################################################
    # ############################## Initial cleanup ##############################
    # #############################################################################

    - name: Delete intrapartition data set if it exists
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ data_set_path }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false

    # #############################################################################
    # ############################## Module Testing ###############################
    # #############################################################################

    - name: Create intrapartition with initial state
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "initial"
      register: res

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ res }}"

    - name: Assert intrapartition Created, changed is true
      ansible.builtin.assert:
        that:
          - res.failed is false
          - res.changed is true
          - res.start_state.exists is false
          - res.end_state.exists is true

    - name: Try recreating same intrapartition
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "initial"
      register: res

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ res }}"

    - name: Assert intrapartition Created, changed is false
      ansible.builtin.assert:
        that:
          - res.failed is false
          - res.changed is false
          - res.start_state.exists is true
          - res.end_state.exists is true

    - name: Remove intrapartition
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "absent"
      register: res

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ res }}"

    - name: Assert intrapartition Removed, changed is true
      ansible.builtin.assert:
        that:
          - res.failed is false
          - res.changed is true
          - res.start_state.exists is true
          - res.end_state.exists is false

    - name: Try removing intrapartition, changed is false
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "absent"
      register: res

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ res }}"

    - name: Assert intrapartition removed, changed is false
      ansible.builtin.assert:
        that:
          - res.failed is false
          - res.changed is false
          - res.start_state.exists is false
          - res.end_state.exists is false
