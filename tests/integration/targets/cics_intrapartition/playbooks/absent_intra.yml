# (c) Copyright IBM Corp. 2023
# Apache License, Version 2.0 (see https://opensource.org/licenses/Apache-2.0)
---
- name: Absent INTRA

  hosts: "all"
  gather_facts: false
  vars:
    intra_name: "DFHINTRA"
    intra_path: "{{ region_data_set_path }}.{{ intra_name }}"

  module_defaults:
    ibm.ibm_zos_cics.intrapartition:
      space_primary: 4
      space_type: "M"
      region_data_sets:
        template: "{{ region_data_set_path }}.<< data_set_name >>"
      cics_data_sets:
        template: "{{ cics_install_path }}.<< lib_name >>"

  tasks:
    # #############################################################################
    # ############################## Initial cleanup ##############################
    # #############################################################################

    - name: Delete INTRA Data Set if it exists
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ intra_path }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false

    # #############################################################################
    # ############################ Initialize Data set #############################
    # #############################################################################

    - name: Run Intrapartition module with Initial target state
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "initial"
      register: initial_result

    - name: Assert Created
      ansible.builtin.assert:
        that:
          - initial_result.failed is false
          - initial_result.changed is true

          - initial_result.start_state.exists == false
          - initial_result.start_state.data_set_organization == "NONE"

          - initial_result.end_state.exists == true
          - initial_result.end_state.data_set_organization == "VSAM"

    # #############################################################################
    # ############################## Module Testing ###############################
    # #############################################################################

    - name: Run Intrapartition module with Absent target state
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.intrapartition:
        state: "absent"
      register: absent_result

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ absent_result }}"

    - name: Assert Removed
      ansible.builtin.assert:
        that:
          - absent_result.failed is false
          - absent_result.changed is true

          - absent_result.start_state.exists == true
          - absent_result.start_state.data_set_organization == "VSAM"

          - absent_result.end_state.exists == false
          - absent_result.end_state.data_set_organization == "NONE"


    # #############################################################################
    # ################################## Cleanup ##################################
    # #############################################################################

    - name: Delete intra data set
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ intra_path }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false
