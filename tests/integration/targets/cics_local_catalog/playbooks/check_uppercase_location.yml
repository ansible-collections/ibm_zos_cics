# (c) Copyright IBM Corp. 2023
# Apache License, Version 2.0 (see https://opensource.org/licenses/Apache-2.0)
---
- name: Check Lowercase Location Parameter

  hosts: "all"
  gather_facts: false
  vars:
    region_dataset_path: "ANSIBIT.TESTS.{{ cics_test_area_initial }}"
    catalog_name: "DFHLCD"

  tasks:
    # #############################################################################
    # ############################## Initial cleanup ##############################
    # #############################################################################

    - name: Delete Catalog Dataset if it exists
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ region_dataset_path }}.LOW.{{ catalog_name }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false

    # #############################################################################
    # ############################## Module Testing ###############################
    # #############################################################################

    - name: Give lowercase LCD location
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.local_catalog:
        location: "{{ region_dataset_path }}.low.{{ catalog_name }}"
        sdfhload: "{{ cics_steplib }}"
        space_primary: 5
        space_type: "M"
        state: "initial"
      register: result
      failed_when: false

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ result }}"

    - name: Debug2
      ansible.builtin.debug:
        msg: "{{ region_dataset_path }}.low.{{ catalog_name }}"

    - name: Debug3
      ansible.builtin.debug:
        msg: "{{ environment_vars }}"


    - name: Assert passed with lowercase
      ansible.builtin.assert:
        that:
          - result.failed is false
          - result.changed is true

          - result.start_state.exists == false
          - result.start_state.vsam == false

          - result.end_state.exists == true
          - result.end_state.vsam == true

    # #############################################################################
    # ################################## Cleanup ##################################
    # #############################################################################

    - name: Delete Catalog Dataset
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ region_dataset_path }}.LOW.{{ catalog_name }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false
