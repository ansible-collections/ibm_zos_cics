# (c) Copyright IBM Corp. 2023
# Apache License, Version 2.0 (see https://opensource.org/licenses/Apache-2.0)
---
- name: Warm Catalog

  hosts: "all"
  gather_facts: false
  vars:
    catalog_name: "DFHLCD"
    catalog_path: "{{ region_data_set_path }}.{{ catalog_name }}"

  module_defaults:
    ibm.ibm_zos_cics.local_catalog:
      cics_data_sets:
        sdfhload: "{{ cics_steplib }}"
      space_primary: 5
      space_type: "M"
      region_data_sets:
        template: "{{ region_data_set_path }}.<< data_set_name >>"

  tasks:
    # #############################################################################
    # ############################## Initial cleanup ##############################
    # #############################################################################

    - name: Delete Catalog Data Set if it exists
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ catalog_path }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false

    # #############################################################################
    # ############################ Initialize Catalog #############################
    # #############################################################################

    - name: Run Local Catalog module with Initial target state
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.local_catalog:
        state: "initial"
      register: initial_result

    - name: Assert Created
      ansible.builtin.assert:
        that:
          - initial_result.failed is false
          - initial_result.changed is true

          - initial_result.start_state.exists == false
          - initial_result.start_state.data_set_organization == "NONE"

          - initial_result.end_state.exists == true
          - initial_result.end_state.data_set_organization == "VSAM"

    # #############################################################################
    # ############################## Module Testing ###############################
    # #############################################################################

    - name: Run Local Catalog module with Warm target state
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.local_catalog:
        state: "warm"
      register: warm_result

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ warm_result }}"

    - name: Assert Removed
      ansible.builtin.assert:
        that:
          - warm_result.failed is false
          - warm_result.changed is false

          - warm_result.start_state.exists == true
          - warm_result.start_state.data_set_organization == "VSAM"

          - warm_result.end_state.exists == true
          - warm_result.end_state.data_set_organization == "VSAM"

    # #############################################################################
    # ################################## Cleanup ##################################
    # #############################################################################

    - name: Delete Catalog Data Set
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ catalog_path }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is changed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false
