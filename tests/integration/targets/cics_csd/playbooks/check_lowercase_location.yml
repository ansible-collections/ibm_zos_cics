# (c) Copyright IBM Corp. 2023
# Apache License, Version 2.0 (see https://opensource.org/licenses/Apache-2.0)
---
- name: Check Lowercase Location Parameter

  hosts: "all"
  gather_facts: false
  vars:
    ds_name: "DFHCSD"

  tasks:
    # #############################################################################
    # ############################## Initial cleanup ##############################
    # #############################################################################

    - name: Delete CSD Data Set if it exists
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ region_data_set_path }}.LOW.{{ ds_name }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is not failed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false

    # #############################################################################
    # ############################## Module Testing ###############################
    # #############################################################################

    - name: Give lowercase CSD location
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_cics.csd:
        region_data_sets:
          dfhcsd:
            dsn: "{{ region_data_set_path }}.low.{{ ds_name }}"
        cics_data_sets:
          sdfhload: "{{ cics_steplib }}"
        space_primary: 5
        space_type: "M"
        state: "initial"
      register: result
      failed_when: false

    - name: Debug
      ansible.builtin.debug:
        msg: "{{ result }}"

    - name: Assert passed with lowercase
      ansible.builtin.assert:
        that:
          - result.failed is false
          - result.changed is true

          - result.start_state.exists == false
          - result.start_state.data_set_organization == "NONE"

          - result.end_state.exists == true
          - result.end_state.data_set_organization == "VSAM"

    # #############################################################################
    # ################################## Cleanup ##################################
    # #############################################################################

    - name: Delete CSD Data Set
      environment: "{{ environment_vars }}"
      ibm.ibm_zos_core.zos_data_set:
        name: "{{ region_data_set_path }}.LOW.{{ ds_name }}"
        state: absent
      register: delete_result
      retries: 3
      until: delete_result is changed

    - name: Assert Delete Passed
      ansible.builtin.assert:
        that:
          - delete_result.failed == false
